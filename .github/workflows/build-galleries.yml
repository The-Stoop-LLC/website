name: Build Drive galleries

on:
  workflow_dispatch:
 
permissions:
  contents: write

concurrency:
  group: build-galleries
  cancel-in-progress: false

jobs:
  regenerate:
    runs-on: ubuntu-latest
    steps:
      - name: Check out main
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install script dependencies
        run: pip install -r scripts/requirements.txt

      - name: Regenerate galleries
        env:
          DRIVE_SA_KEY: ${{ secrets.DRIVE_SA_KEY }}
          DEBUG_GALLERY: "1"
        run: python3 scripts/build_galleries.py

      # Drive thumbnails are rate-limited and not a CDN, so every image and
      # video poster the galleries reference is copied into the repo, then
      # given a small WebP thumbnail for the tile.
      - name: Localize Drive images and posters
        run: python3 scripts/localize_drive_images.py

      - name: Build WebP thumbnails
        run: python3 scripts/make_thumbs.py

      - name: Commit and push if changed
        run: |
          if git diff --quiet; then
            echo "No gallery changes."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Regenerate Drive galleries

          Automated run of scripts/build_galleries.py. Picks up any new or
          renamed files in the Drive folders referenced by DRIVE_GALLERY
          markers across the site."
          git push origin main
